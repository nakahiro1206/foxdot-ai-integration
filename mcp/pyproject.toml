[project]
name = "mcp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "foxdot>=0.9.0",
    "gradio>=6.6.0",
    "python-dotenv>=1.2.1",
    "watchdog>=6.0.0",
]
